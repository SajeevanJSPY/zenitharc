<div class="bg-base-200 py-10 px-6">
  <div class="my-20">
    <div class="max-w-3xl mx-auto text-center">
      <h1 class="text-4xl font-bold text-primary mb-4">Welcome to the Arc Admin Panel</h1>
      <p class="text-base-content/70 mb-8">
        Oversee users, roles, and transaction finalization from a centralized dashboard.
      </p>

      <% if arc_signed_in? %>
        <div class="mb-6">
          <p class="text-sm text-base-content/60">
            Logged in as <strong><%= current_arc.email %></strong>
          </p>
        </div>
        <div class="flex justify-center gap-4">
          <%= link_to "Go to Dashboard", arc_dashboard_path, class: "btn btn-primary" %>
          <%= button_to "Logout", arc_logout_path, method: :delete, class: "btn btn-outline btn-error" %>
        </div>
      <% else %>
        <div class="space-x-4">
          <%= link_to "Login", arc_login_path, class: "btn btn-primary" %>
          <%= link_to "Register", arc_register_path, class: "btn btn-outline btn-secondary" %>
        </div>
      <% end %>
    </div>

    <div class="mt-16 max-w-4xl mx-auto grid grid-cols-1 md:grid-cols-3 gap-6">
      <div class="p-6 rounded-xl bg-base-100 shadow-md">
        <h2 class="text-xl font-semibold mb-2 text-primary">Manage Roles</h2>
        <p class="text-sm text-base-content/70">Assign and manage roles for Arc and Customer accounts securely.</p>
      </div>
      <div class="p-6 rounded-xl bg-base-100 shadow-md">
        <h2 class="text-xl font-semibold mb-2 text-secondary">User Monitoring</h2>
        <p class="text-sm text-base-content/70">Access and analyze user activities with real-time data.</p>
      </div>
      <div class="p-6 rounded-xl bg-base-100 shadow-md">
        <h2 class="text-xl font-semibold mb-2 text-accent">Transaction Control</h2>
        <p class="text-sm text-base-content/70">Finalize or inspect transactions to ensure system integrity.</p>
      </div>
    </div>
  </div>
</div>
